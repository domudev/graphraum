---
title: Software topology demo
description: Render applications, services, gateways, and databases as an interactive dependency graph.
---

import UseCaseGallery from "../../../components/UseCaseGallery.astro";
import VersionReference from "../../../components/VersionReference.astro";

<VersionReference />

This graph separates applications, services, gateways, and databases through batched shape and color encodings. Select a component to inspect ownership and status, trace its blast radius, or switch the same data between 2D and 3D.

<UseCaseGallery useCaseId="dependencies" />

Explore the [knowledge graph](../knowledge/) and [fraud investigation](../investigation/) demos, or return to the [use-case overview](../).
