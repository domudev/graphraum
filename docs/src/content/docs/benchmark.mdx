---
title: Playground
description: Run the timed graphraum proof protocol in this browser. Open the fullscreen Explorer for live layouts and appearance.
template: splash
tableOfContents: false
---

import LiveBenchmark from "../../components/LiveBenchmark.astro";

<div class="proof-intro">
  <p class="proof-kicker">Executable evidence</p>
  <h1>Prove the cadence on this device.</h1>
  <p class="proof-lede">Run the deterministic timing protocol locally. For live layouts and appearance, open the fullscreen <a href="../explore/">Explorer</a>. No uploaded traces. No favorable-machine screenshot.</p>
</div>

<div class="reference-run not-content" aria-label="Reference result from a 100,000 node benchmark">
  <div>
    <span class="reference-label">Reference run · 100k nodes / 300k edges</span>
    <strong>8.3 ms</strong>
    <span>median frame cadence · 2D and 3D</span>
  </div>
  <div>
    <span class="reference-label">First meaningful frame</span>
    <strong>395 / 305 ms</strong>
    <span>2D / 3D</span>
  </div>
  <div>
    <span class="reference-label">1% incremental update</span>
    <strong>9.1 / 10.2 ms</strong>
    <span>2D / 3D · 1,000 nodes</span>
  </div>
  <div>
    <span class="reference-label">GPU work</span>
    <strong>2 calls</strong>
    <span>100k visible edge budget</span>
  </div>
</div>

<p class="reference-context">Chrome 150 on macOS, DPR 2 capped to 1, captured 21 July 2026. Treat it as a reference, not a universal score—run the same Prove protocol below on your device.</p>

<LiveBenchmark />

## Explorer

Open the [fullscreen Explorer](../explore/) for layouts (grid, circle, force, force live) and appearance controls. That surface is for visual verification, not a published score.

## Prove

- **Phases:** fixture generation, renderer construction, synchronous `setData`, and the first visible frame are reported separately.
- **First meaningful frame:** the complete fixture-through-first-frame path for compatibility with earlier results.
- **Display cadence:** 30 idle animation frames establish the device and display baseline.
- **Frame times:** 120 explicitly scheduled renders at 1k/10k and 30 at 100k, reported as p50, p95, and max.
- **Long tasks:** browser Performance Observer entries over 50ms during the run.
- **Selection feedback:** 20 selection changes at 1k/10k and 5 at 100k, measured through the following animation frame.
- **Progressive force layout:** worker force compute plus transferable `applyLayout` batches. Reported under `phases.layout` as startup, transfer, compute, first useful frame, and complete. Force compute is one blocking worker burst; progressive batches stream positions to the canvas afterward.
- **Incremental update:** position changes for 1% of existing nodes, including only their node matrices and incident edge endpoints.
- **Full update:** replacement with a graph containing 1% more nodes and three edges per node.
- **GPU draw calls:** Three.js renderer calls from the final frame.
- **Picking strategy:** spatial-grid lookup in 2D or instanced raycasting in 3D.
- **Viewport and LOD:** visible node count, incident edge candidates, rendered edge count, and active detail/overview tier.
- **Heap growth:** Chrome's non-standard precise heap counter when available; `null` elsewhere.

## Interpret honestly

Prove is primarily a rendering microbenchmark. Progressive force layout timing is included so layout cost is visible beside renderer cadence; it is not a claim that layout has converged or that Rust/Wasm is required. The 100k fixture intentionally allocates a large in-memory graph and may expose long tasks while full-snapshot updates remain synchronous. Keep the tab visible, avoid DevTools throttling unless that is the experiment, and compare repeated fresh-page runs on the same browser and effective pixel ratio.
